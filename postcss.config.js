module.exports = {
    plugins: [
        require('autoprefixer')
    ],
    // 配置autoprefix
    browsers: [
        "iOS >= 8",
        "Firefox >= 20",
        "Android > 4.4",
        "ie >= 9"
    ]
}